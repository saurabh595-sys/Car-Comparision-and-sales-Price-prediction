// This file was auto-generated by ML.NET Model Builder. 

using System;
using WebApplication1ML.Model;

namespace WebApplication1ML.ConsoleApp
{
    class Program
    {
        static void Main(string[] args)
        {
            // Create single instance of sample data from first line of dataset for model input
            ModelInput sampleData = new ModelInput()
            {
                Name = carname.SelectedItem.Value,
                Company = company.SelectedItem.Value,
                Year = float.Parse(year.SelectedItem.Value),
                Kms_driven = 45000F,
                Fuel_type = fuletype.SelectedItem.Value,
            };

            // Make a single prediction on the sample data and print results
            var predictionResult = ConsumeModel.Predict(sampleData);

            Console.WriteLine("Using model to make single prediction -- Comparing actual Price with predicted Price from sample data...\n\n");
            Console.WriteLine($"Name: {sampleData.Name}");
            Console.WriteLine($"Company: {sampleData.Company}");
            Console.WriteLine($"Year: {sampleData.Year}");
            Console.WriteLine($"Kms_driven: {sampleData.Kms_driven}");
            Console.WriteLine($"Fuel_type: {sampleData.Fuel_type}");
            Console.WriteLine($"\n\nPredicted Price: {predictionResult.Score}\n\n");
            Console.WriteLine("=============== End of process, hit any key to finish ===============");
            Console.ReadKey();
        }
    }
}
